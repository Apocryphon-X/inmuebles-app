import React from 'react';
import styles from '../styles/Footer.module.css';

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>
      <p>Información de la página, redes sociales y contacto</p>
    </footer>
  );
};

export default Footer;

